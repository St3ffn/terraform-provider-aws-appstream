name: Tool Commitlint

on:
  pull_request:
    paths:
      - '.golangci.yaml'
      - 'tools/commitlint/**'
      - '.github/workflows/tools-commitlint.yml'
      - '!tools/commitlint/README.md'
  push:
    branches:
      - main
    paths:
      - '.golangci.yaml'
      - 'tools/commitlint/**'
      - '.github/workflows/tools-commitlint.yml'
      - '!tools/commitlint/README.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Commitlint Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: tools/commitlint
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'tools/commitlint/go.mod'
          cache: true
      - name: Run Linters
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: latest
          working-directory: tools/commitlint
      - run: go mod download
      - run: go test -v -cover -timeout 10m ./...
